name: CI

on:
  push:
    branches: [main]
    paths:
      - "apps/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "turbo.json"
      - ".github/workflows/ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "apps/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "turbo.json"
      - ".github/workflows/ci.yml"

env:
  DATABASE_URL: file:./dev.db

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 12.4.1

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @repo/db db:generate

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test

      - name: Build
        run: pnpm build

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build image and boot feature-flag-admin
        run: |
          docker compose -f apps/feature-flag-admin/docker-compose.yml up -d --build

      - name: Smoke test the running container
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:3001/flags | grep -q "checkout.new_ui"; then
              echo "App is serving seeded flags"; exit 0
            fi
            sleep 2
          done
          echo "App did not become ready in 60s"; exit 1

      - name: Dump container logs on failure
        if: failure()
        run: docker compose -f apps/feature-flag-admin/docker-compose.yml logs

      - name: Tear down
        if: always()
        run: docker compose -f apps/feature-flag-admin/docker-compose.yml down -v
