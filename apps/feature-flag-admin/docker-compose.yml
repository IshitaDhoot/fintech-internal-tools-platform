services:
  migrate:
    build:
      context: ../..
      dockerfile: apps/feature-flag-admin/Dockerfile
      target: migrator
    environment:
      DATABASE_URL: file:/data/dev.db
    volumes:
      - sqlite-data:/data

  app:
    build:
      context: ../..
      dockerfile: apps/feature-flag-admin/Dockerfile
      target: runner
    ports:
      - "3001:3000"
    environment:
      DATABASE_URL: file:/data/dev.db
    volumes:
      - sqlite-data:/data
    depends_on:
      migrate:
        condition: service_completed_successfully

volumes:
  sqlite-data:
